<!--pages/indexed/indexed.wxml-->
<view class="right-nav">
  <view bindtap="getCurrentCode" class="{{chooseIndex == index ? '.city-list-active' : ''}} cityLiist"  wx:for="{{cityList}}"  data-code="{{item.code}}" data-index="{{index}}">
    {{item.code}}
    </view>
</view>
 
 <view class="city-layer {{isShowLayer ? '' : 'layer-hide'}}">
  {{code}}
</view>  

